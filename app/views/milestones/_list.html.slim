- columns = [:id, :title, :estimated_time, :description]

- if @milestones.present?
  table.table.table-striped
    thead
      - columns.each_with_index do |col, i|
        th(style="#{'width: 40px;' if i == 0}")
          = col
      th

    tbody
      - @milestones.each do |p|
        tr
          td
            = p.id
          td
            = link_to p, organization_milestone_path(organization, p)
          td
            = hours p.estimated_time
          td
            = p.description
          td.text-right
            = link_to 'edit', edit_organization_milestone_path(organization, p), class: 'btn btn-link'
            = link_to 'delete', organization_milestone_path(organization, p), method: :delete, confirm: "Удалить этап #{p}?", class: 'btn btn-link'


- else
  | Пока ничего нет
